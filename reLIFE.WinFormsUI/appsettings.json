{
    "ConnectionStrings": {
        // Use this if your Windows login has access to the database
        "DefaultConnection": "Server=a4\\SQLEXPRESS;Database=reLifeDB;Integrated Security=True;TrustServerCertificate=True;Encrypt=Optional;"
    },
    "Logging": {
        // Basic logging configuration (can be expanded later if needed)
        "LogLevel": {
            "Default": "Information", // Set default logging level
            "Microsoft": "Warning", // Reduce noise from Microsoft components
            "Microsoft.Hosting.Lifetime": "Information" // Useful startup/shutdown messages
        }
    },
    "AllowedHosts": "*" // Standard setting, less relevant for desktop apps
}